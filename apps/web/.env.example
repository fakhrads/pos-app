# URL API backend (Bun + Elysia). Default untuk dev lokal.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Opsional: URL publik aplikasi (dipakai untuk absolute links, dsb.)
NEXT_PUBLIC_APP_URL=http://localhost:3000
